apiVersion: v1
kind: Service
metadata:
  name: developer-self-service-frontend
  namespace: default
  labels:
    app: developer-self-service-frontend
    component: frontend
    managed-by: developer-self-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: developer-self-service-frontend

